/* bop Aero embed: aircraft-calc
 * Built 2026-09-15 from components/aircraft-calc/src/ — DO NOT EDIT THIS FILE.
 * Edit the source or a data/<id>.json, then: python3 tools/build.py aircraft-calc
 * Aircraft in this build: sf50, sr22t
 */
(function () {
  var NAME = "aircraft-calc";
  var STYLE_ID = 'bopaero-style-' + NAME;
  var CSS = "/* ============================================================================\n   bop Aero embed design tokens \u2014 ONE definition, prepended to every component\n   by tools/build.py. Edit here, rebuild, and both components change together.\n\n   WHY THIS FILE EXISTS: the components previously declared almost no colours or\n   fonts, so every unspecified property silently inherited whatever Squarespace\n   supplied. That produced two customer-facing bugs in one week - orange footnotes\n   and an orange full-size usage note - each fixed one at a time. Tokens make the\n   whole class of bug impossible: nothing is left to inherit by accident.\n\n   Values are MEASURED from bopaero.com computed styles (2026-09-15), not guessed.\n   ========================================================================== */\n.bop-embed {\n  /* brand ------------------------------------------------------------------ */\n  --bop-orange:        #FF6700;\n  --bop-orange-ink:    #fff;\n  /* Derived, so a brand tweak needs one edit. color-mix keeps hover/active in\n     step with --bop-orange instead of drifting as hand-picked hex values. */\n  --bop-orange-hover:  color-mix(in srgb, var(--bop-orange) 88%, #000);\n  --bop-orange-soft:   color-mix(in srgb, var(--bop-orange) 12%, #fff);\n  --bop-navy:          #003767;\n  --bop-ink:           #282D30;\n\n  /* surfaces --------------------------------------------------------------- */\n  --bop-panel:         #E9CEB0;   /* the established embed panel - kept for continuity */\n  --bop-card:          #fff;\n  --bop-line:          #e3e3e3;\n\n  /* text ------------------------------------------------------------------- */\n  --bop-text:          #333;\n  --bop-text-muted:    #6b6b6b;\n\n  /* type ------------------------------------------------------------------- */\n  --bop-font-head: \"Archivo Black\", \"Arial Black\", system-ui, sans-serif;\n  --bop-font-body: proxima-nova, \"Proxima Nova\", -apple-system, BlinkMacSystemFont,\n                   \"Segoe UI\", Helvetica, Arial, sans-serif;\n  --bop-head-ls:   -0.02em;       /* the site sets a tight negative tracking */\n\n  /* geometry --------------------------------------------------------------- */\n  --bop-radius:      10px;\n  --bop-radius-pill: 300px;       /* site buttons are full pills */\n  --bop-gap:         12px;\n\n  /* feedback --------------------------------------------------------------- */\n  --bop-focus:       var(--bop-navy);\n  --bop-warn-text:   #7a4a00;\n  --bop-warn-bg:     #fff6e5;\n  --bop-warn-edge:   #e8820c;\n\n  --bop-shadow:      0 1px 3px rgba(0,0,0,.10), 0 6px 18px rgba(0,0,0,.06);\n}\n\n/* Motion is opt-out, not opt-in. */\n@media (prefers-reduced-motion: reduce) {\n  .bop-embed *, .bop-embed *::before, .bop-embed *::after {\n    animation-duration: .001ms !important;\n    transition-duration: .001ms !important;\n  }\n}\n\n/* ---- Collapsible summary styles ---- */\n    .calc-accordion {\n      container-type: inline-size;\n      container-name: bopcalc;                                                                                                                                    \n      border: none;\n      margin: 0 0 12px 0;                                                                                                                                \n    }                                                       \n    .calc-summary {                                                                                                                                      \n      list-style: none;\n      cursor: pointer;                                                                                                                                   \n      user-select: none;                                    \n      display: flex;\n      align-items: center;\n      gap: 12px;                                                                                                                                         \n      padding: 12px 14px;\n      background: var(--bop-orange);                                                                                                                               \n      color: #fff;                                          \n      border-radius: 10px;\n      box-shadow: 0 0 10px rgba(0,0,0,.25);                                                                                                              \n      outline: none;\n    }                                                                                                                                                    \n    .calc-summary::-webkit-details-marker { display: none; }                                                                                             \n   \n    .summary-title {                                                                                                                                     \n      font-size: 18px;                                      \n      font-weight: 700;                                                                                                                                  \n      flex: 1;\n    }                                                                                                                                                    \n    .summary-hint {                                         \n      font-size: 18px;\n      font-weight: 500;\n      opacity: .85;                                                                                                                                      \n    }\n    .summary-caret {                                                                                                                                     \n      width: 10px;                                          \n      height: 10px;\n      border-right: 2px solid #fff;\n      border-bottom: 2px solid #fff;                                                                                                                     \n      transform: rotate(-45deg);\n      transition: transform .2s ease;                                                                                                                    \n      margin-left: 6px;                                     \n    }                                                                                                                                                    \n    .calc-accordion[open] .summary-caret { transform: rotate(45deg); }\n    .calc-summary:hover { background: var(--bop-orange-hover); }                                                                                                         \n    .calc-summary:focus-visible {                                                                                                                        \n      outline: 2px solid var(--bop-focus);                                                                                                                        \n      outline-offset: 2px;                                                                                                                               \n    }                                                                                                                                                    \n                                                            \n    .calculator-container {                                                                                                                              \n      font-family: var(--bop-font-body);\n      max-width: 1100px;                                                                                                                                 \n      margin: auto;                                         \n      padding: 15px;\n      background: var(--bop-panel);\n      border-radius: 10px;                                                                                                                               \n      box-shadow: 0 0 15px rgba(0,0,0,0.3);\n      margin-top: 12px;                                                                                                                                  \n    }                                                       \n                                                                                                                                                         \n    .calculator-container h2 {                              \n      text-align: center;\n      color: var(--bop-text);                                                                                                                                       \n      font-size: 30px;\n      margin-bottom: 15px;                                                                                                                               \n    }                                                       \n\n    .calculator-grid {\n      display: grid;\n      grid-template-columns: 1fr 1fr;\n      gap: 20px;                                                                                                                                         \n      align-items: start;\n    }                                                                                                                                                    \n                                                            \n    .image-panel {\n      background: transparent;\n      border: none;                                                                                                                                      \n      border-radius: 8px;\n      box-shadow: none;                                                                                                                                  \n      padding: 0;                                           \n      overflow: hidden;\n      display: flex;                                                                                                                                     \n      flex-direction: column;\n      gap: 125px;                                                                                                                                        \n    }                                                       \n    .image-panel img {\n      display: block;\n      width: 100%;\n      height: auto;                                                                                                                                      \n      object-fit: cover;\n      border-radius: 8px;                                                                                                                                \n    }                                                       \n\n    .calculator-container h3 {                                                                                                                           \n      text-align: center;\n      font-size: 28px;                                                                                                                                   \n      color: var(--bop-text);                                          \n      margin-bottom: 10px;\n    }\n\n    .calculator {                                                                                                                                        \n      display: flex;\n      flex-direction: column;                                                                                                                            \n      gap: 20px;                                            \n    }\n\n    .calculator .input-section,                                                                                                                          \n    .calculator .button-section,\n    .calculator .output-section {                                                                                                                        \n      background: #fff;                                     \n      padding: 15px;\n      border: 1px solid var(--bop-line);\n      border-radius: 8px;\n      box-shadow: 0 1px 2px rgba(0,0,0,0.3);                                                                                                             \n    }\n                                                                                                                                                         \n    .calculator label {                                                                                                                                  \n      display: flex;\n      flex-direction: column;                                                                                                                            \n      font-size: 18px;                                      \n      font-weight: bold;\n      color: #555;\n    }\n    .calculator input {                                                                                                                                  \n      padding: 10px;\n      margin-top: 8px;                                                                                                                                   \n      border: 1px solid var(--bop-line);                               \n      border-radius: 6px;\n      font-size: 24px;                                                                                                                                   \n      text-align: center;\n    }                                                                                                                                                    \n                                                            \n    .calculator .button-section {\n      display: flex;\n      gap: 10px;\n      justify-content: center;                                                                                                                           \n    }\n    /* The share selector must read as a sibling of the number inputs, not as a\n       raw OS control dropped into a branded panel. */\n    .calculator select#share-count {\n      width: 100%;\n      padding: 10px;\n      font-size: 24px;\n      text-align: center;\n      font-family: var(--bop-font-body);\n      color: var(--bop-ink);\n      background: var(--bop-card);\n      border: 1px solid var(--bop-line);\n      border-radius: var(--bop-radius);\n      appearance: none;\n      cursor: pointer;\n    }\n    .calculator select#share-count:focus-visible {\n      outline: 2px solid var(--bop-focus);\n      outline-offset: 2px;\n    }\n\n    .calculator .button-section button {                                                                                                                 \n      padding: 10px 20px;                                   \n      border: none;\n      border-radius: 6px;\n      font-size: 16px;\n      cursor: pointer;                                                                                                                                   \n      transition: background-color 0.3s;\n      color: #fff;                                                                                                                                       \n    }                                                       \n    /* Was Bootstrap red/green (#dc3545 / #28a745) - stock colours that appear\n       nowhere else on bopaero.com, sitting at the exact point of interaction.\n       Now the site's own language: a filled orange pill for the primary action,\n       a quiet outlined pill for the destructive one. */\n    .calculator .button-section button {\n      font-family: var(--bop-font-body);\n      font-weight: 600;\n      border-radius: var(--bop-radius-pill);\n      padding: 12px 26px;\n      transition: background .15s ease, color .15s ease, border-color .15s ease;\n    }\n    .calculator .button-section button:last-child {           /* Calculate */\n      background: var(--bop-orange);\n      color: var(--bop-orange-ink);\n      border: 2px solid var(--bop-orange);\n    }\n    .calculator .button-section button:last-child:hover {\n      background: var(--bop-orange-hover);\n      border-color: var(--bop-orange-hover);\n    }\n    .calculator .button-section button:first-child {          /* Clear */\n      background: transparent;\n      color: var(--bop-text-muted);\n      border: 2px solid var(--bop-line);\n    }\n    .calculator .button-section button:first-child:hover {\n      background: var(--bop-orange-soft);\n      color: var(--bop-ink);\n      border-color: var(--bop-orange);\n    }\n    .calculator .button-section button:focus-visible {\n      outline: 2px solid var(--bop-focus);\n      outline-offset: 2px;\n    }\n                                                                                                                                                         \n    .calculator .output-item {                                                                                                                           \n      display: flex;\n      justify-content: space-between;                                                                                                                    \n      align-items: baseline;                                \n      padding: 8px 0;\n      border-bottom: 1px solid var(--bop-line);\n      color: #555;\n    }\n    .calculator .output-item.share-hours span {\n      font-size: 18px;                                                                                                                                   \n      font-weight: bold;\n    }                                                                                                                                                    \n    .calculator .output-item span:last-child {              \n      text-align: right;\n      display: inline-block;\n      width: 50%;\n    }                                                                                                                                                    \n    .calculator .output-item:last-child { border-bottom: none; }\n                                                                                                                                                         \n    /* No colour was set, so the footnotes inherited Squarespace body copy and\n       rendered orange - louder than the figures they annotate. #333 is the same\n       near-black the output rows already use, so the footnotes read as quiet\n       supporting text rather than outweighing the numbers above them. */\n    .calculator .addendum {\n      padding: 12px;\n      font-size: 12px;\n      font-style: italic;\n      color: var(--bop-text);\n    }\n\n    /* The prose is injected as innerHTML from addendumHtml, so its <p> children\n       can pick the inherited colour back up; pin them too. */\n    .calculator .addendum p { color: var(--bop-text); }                                                                                                                                                    \n   \n    .calculator span { font-size: 16px; }\n\n    /* The site sets headings in Archivo Black with tight negative tracking. */\n    .calculator h2, .calculator h3, .calculator .calc-heading {\n      font-family: var(--bop-font-head);\n      letter-spacing: var(--bop-head-ls);\n    }                                                                                                                \n                                                            \n    .second-image-mobile { display: none; }\n\n    /* Container query, not a media query. These are embeds: the block can sit in a\n       narrow column on a wide screen, where @media (max-width:900px) never fires and\n       the two-column grid would be crushed. @container measures the component. */\n    @container bopcalc (max-width: 900px) {\n      .calculator-grid { grid-template-columns: 1fr; }\n      .image-panel .second-image-desktop { display: none; }                                                                                              \n      .second-image-mobile {\n        display: block;                                                                                                                                  \n        margin-top: 15px;                                   \n      }\n      .second-image-mobile img {\n        width: 100%;                                                                                                                                     \n        border-radius: 8px;\n      }                                                                                                                                                  \n    }\n\n/* The [hidden] attribute must actually hide.\n   `.calculator .output-item { display: flex }` above has higher specificity than\n   the browser's built-in `[hidden] { display: none }`, so rows the engine hides\n   (the usage row for the OTHER usage model, or an unavailable share count) stayed\n   visible. Only surfaced once one template served both aircraft. */\n.calculator .output-item[hidden],\n.calculator [hidden] {\n  display: none !important;\n}\n\n\n/* The inline usage note under the inputs. It had NO rule of its own, so it\n   inherited Squarespace's body copy - full-size orange paragraph text that read\n   as loudly as the heading and competed with the Calculate button. It is a\n   clarifying aside, not a warning: keep it quiet so the amber .usage-warning\n   below stays the only thing that shouts. */\n.calculator .input-note {\n  margin: 10px 0 0;\n  font-size: 13px;\n  line-height: 1.45;\n  font-style: italic;\n  color: var(--bop-text-muted);\n}\n\n/* Shown when requested hours exceed a capped program's per-share limit. */\n.calculator .usage-warning {\n  margin: 10px 0 0;\n  padding: 10px 12px;\n  font-size: 13px;\n  line-height: 1.45;\n  color: var(--bop-warn-text);\n  background: var(--bop-warn-bg);\n  border-left: 4px solid var(--bop-warn-edge);\n  border-radius: 4px;\n}\n";
  var HTML = "<!-- Collapsible wrapper. OPEN by default: collapsed, most visitors never reached\n       the numbers, and the calculator is the strongest engagement tool on the page. -->                                                                                                     \n  <details class=\"bop-embed calc-accordion\" data-calc-root open>                                                                                                        \n    <summary class=\"calc-summary\">                                                                                                                       \n      <span class=\"summary-title\"></span>                                                                  \n      <span class=\"summary-hint\">Open</span>                                                                                                             \n      <span class=\"summary-caret\" aria-hidden=\"true\"></span>\n    </summary>                                                                                                                                           \n                                                            \n    <div class=\"calculator-container\">                                                                                                                   \n      <h2></h2>       \n                                                                                                                                                         \n      <!-- Two-column layout for desktop/tablet; stacks on mobile -->                                                                                    \n      <div class=\"calculator-grid\">                                                                                                                      \n        <!-- LEFT: Image column (on mobile this appears above the calculator) -->                                                                        \n        <div class=\"image-panel\">                                                                                                                        \n          <img\n            data-img=\"0\"                                                                                                                          \n            src=\"https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/52dda135-bca3-4b6b-ad19-6a3b94b81807/DSC00966_croatia-a2a_013.jpeg?content-type=image%2Fjpeg\"                                                                                                                              \n            alt=\"\"                                                                                                                        \n            loading=\"lazy\"                                                                                                                               \n          />                                                \n          <img                                                                                                                                           \n            data-img=\"1\"                           \n            src=\"https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/4aad4998-5f44-4e12-8170-7356bfd777d3/FL+Keys+Vision+Jet+Lifestyle.jpeg?content-type=image%2Fjpeg\"                                                                                                                          \n            alt=\"\"\n            loading=\"lazy\"                                                                                                                               \n            class=\"second-image-desktop\"                    \n          />                                                                                                                                             \n        </div>\n                                                                                                                                                         \n        <!-- RIGHT: Calculator -->                          \n        <div class=\"calculator\">\n\n          <!-- Inputs -->                                                                                                                                \n          <div class=\"input-section\">\n            <label for=\"ownership-years\">                                                                                                                \n              Years of Ownership:                           \n              <input\n                type=\"number\"                                                                                                                            \n                id=\"ownership-years\"\n                name=\"ownership-years\"                                                                                                                   \n                inputmode=\"numeric\"                         \n                placeholder=\"e.g., 5 yrs.\"\n                min=\"1\"                                                                                                                                  \n                max=\"20\"\n                step=\"1\"                                                                                                                                 \n                required                                    \n              />\n            </label>\n            <label for=\"share-count\" style=\"margin-top:12px;\">\n              Number of Shares:\n              <select id=\"share-count\" name=\"share-count\"></select>\n            </label>\n            <label for=\"hours-per-year\" style=\"margin-top:12px;\">\n              Estimated Flying Hours per Year:\n              <input\n                type=\"number\"\n                id=\"hours-per-year\"\n                name=\"hours-per-year\"\n                inputmode=\"decimal\"\n                placeholder=\"\"\n                min=\"1\"\n                step=\"any\"\n                required\n              />\n            </label>\n            <p class=\"input-note\" id=\"input-note\" hidden></p>                                                                                                                                     \n          </div>                                            \n\n          <div class=\"button-section\">\n            <button type=\"button\" id=\"clear-calculator\">Clear</button>\n            <button type=\"button\" id=\"calculate-cost\">Calculate</button>                                                                                             \n          </div>\n                                                                                                                                                         \n          <div class=\"output-section\">                      \n            <h3 id=\"share-position\"></h3>\n\n            <div class=\"output-item shares-available\" data-shares hidden>\n              <span>Shares Available:</span>\n              <span id=\"shares-available\">-</span>\n            </div>\n            <div class=\"output-item share-usage\" data-usage=\"unlimited\">\n              <span>Usage:</span>\n              <span id=\"share-usage\">-</span>\n            </div>\n<div class=\"output-item share-hours\" data-usage=\"capped\" hidden>\n              <span>Usage:</span>\n              <span id=\"share-hours\">-</span>\n            </div>\n            <div class=\"output-item scheduling-limit\" data-usage=\"unlimited\">\n              <span>Scheduling Limit:</span>\n              <span id=\"scheduling-limit\">-</span>\n            </div>\n            <div class=\"output-item\">\n              <span>Program Cost:*</span>\n              <span id=\"program-cost\">-</span>\n            </div>\n            <div class=\"output-item\">\n              <span>Annual Program Fee:**</span>\n              <span id=\"annual-program-fee\">-</span>\n            </div>\n            <div class=\"output-item\">\n              <span>Average Fuel/Oil Cost per Hour:***</span>\n              <span id=\"fuel-cost\">-</span>\n            </div>\n\n            <!-- Blended (ownership-only) -->\n            <div class=\"output-item\" id=\"blended-row\">\n              <span>Estimated Ownership Cost per Hour:****</span>\n              <span id=\"blended-cost-per-hour\">-</span>\n            </div>\n                                                                                                                                                         \n            <div class=\"usage-warning\" id=\"usage-warning\" hidden></div>\n\n            <!-- MOBILE: second image appears below the results -->                                                                                      \n            <div class=\"second-image-mobile\">\n              <img data-img=\"1\"                                                                                                                                       \n                src=\"https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/4aad4998-5f44-4e12-8170-7356bfd777d3/FL+Keys+Vision+Jet+Lifestyle.jpeg?content-type=image%2Fjpeg\"                                                                                                                      \n                alt=\"\"\n                loading=\"lazy\"                                                                                                                           \n              />                                            \n            </div>                                                                                                                                       \n                                                            \n            <div class=\"addendum\"></div>\n          </div>\n        </div>\n      </div>\n    </div>\n  </details>\n";
  var DATA = {
"sf50": {
"id": "sf50",
"summaryTitle": "Cirrus SF50 Vision Jet Shared Ownership Cost Calculator",
"heading": "2026 Cirrus SF50 G3 Vision Jet Shared Ownership Program",
"sharePositionLabel": "Ownership Costs (per share)",
"totalShares": 9,
"programCost": 544375,
"annualProgramFee": 136178.438,
"fuel": {
"label": "JET-A",
"priceKey": "jet_a",
"gph": 59,
"oilPerHour": 6.45,
"fallbackCostPerHour": 375
},
"usage": {
"model": "unlimited",
"schedulingHours": 175,
"schedulingDays": 35
},
"images": [
{
"src": "https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/52dda135-bca3-4b6b-ad19-6a3b94b81807/DSC00966_croatia-a2a_013.jpeg?content-type=image%2Fjpeg",
"alt": "SF50 Vision Jet"
},
{
"src": "https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/4aad4998-5f44-4e12-8170-7356bfd777d3/FL+Keys+Vision+Jet+Lifestyle.jpeg?content-type=image%2Fjpeg",
"alt": "SF50 Cockpit"
}
],
"availableShares": 8,
"_dataVerified": "Annual program fee corrected by Raymond 2026-09-12 (per share). totalShares 9 / availableShares 8 also confirmed by Raymond. Program cost, usage and fuel figures carried from live and NOT re-confirmed.",
"addendumHtml": "<p>*{{totalShares}} total shared ownership positions, {{availableShares}} available for purchase{{remainingClause}}. The Program Cost purchases one position. Each share includes unlimited usage. For scheduling purposes, a share may reserve up to {{schedulingHours}} flight hours or {{schedulingDays}} days at one time. Program longevity for each aircraft is approximately ten (10) years. The aircraft will then be sold and the proceeds used to purchase a new aircraft. Applicable taxes related to the aircraft purchase are not included.</p>\n <p>**The Annual Program Fee includes the bop Aero management fee, estimated aircraft maintenance, and fixed costs including insurance and storage.</p>\n <p id=\"fuel-note\">***{{fuelLabel}} national avg \u00d7 {{gph}} gph + oil.</p>\n <p>****Estimated cost per hour does not include operational costs such as fuel or a pilot (if applicable). Year 1 includes the Program Cost and Annual Program Fee; additional years include the Annual Program Fee only. Estimated cost per hour does not factor the eventual sale of the ownership share, which may reduce the owner\u2019s realized net cost.</p>",
"inputNote": "Usage is unlimited. For scheduling purposes, each share may reserve up to {{schedulingHours}} flight hours or {{schedulingDays}} days at one time.",
"map": {
"cities": [
{
"name": "Boca Raton",
"lat": 26.3587,
"lng": -80.0831
},
{
"name": "Dallas",
"lat": 32.7767,
"lng": -96.797
},
{
"name": "Cincinnati",
"lat": 39.1031,
"lng": -84.512
},
{
"name": "Atlanta",
"lat": 33.749,
"lng": -84.388
},
{
"name": "Denver",
"lat": 39.7392,
"lng": -104.9903
},
{
"name": "Providence",
"lat": 41.824,
"lng": -71.4128
},
{
"name": "Raleigh",
"lat": 35.7796,
"lng": -78.6382
}
],
"serviceArea": {
"model": "split",
"dividerLongitude": -100,
"legendTitle": "Program Area",
"legendText": "Bi-coastal U.S. operation, divided into Western and Eastern program halves.",
"dotLabel": "Owner / interest location",
"fitBounds": [
[
24.5,
-125.0
],
[
49.5,
-66.5
]
],
"legendNote": "The aircraft stays where the last owner left it and repositions to the next owner, avoiding empty repositioning cycles."
},
"_note": "Cities are LOCATIONS SHOWING INTEREST \u2014 that is the purpose of this map (Raymond, 2026-09-12). Only Denver currently sits west of the divider; that imbalance is real data, NOT a bug. Do not invent western cities to balance it. Divider is the 100th meridian, confirmed by Raymond."
},
"sharesRemaining": 8,
"hoursPlaceholder": "e.g., 175 hrs."
},
"sr22t": {
"id": "sr22t",
"summaryTitle": "Cirrus SR22T Shared Ownership Cost Calculator",
"heading": "2026 Cirrus SR22T G7+ GTS Shared Ownership Program",
"sharePositionLabel": "Ownership Cost (per share)",
"totalShares": 16,
"programCost": 100955.996,
"annualProgramFee": 33975.783,
"fuel": {
"label": "100LL",
"priceKey": "avgas_100ll",
"gph": 18,
"oilPerHour": 2.0,
"fallbackCostPerHour": 110
},
"usage": {
"model": "capped",
"maxHours": 96
},
"images": [
{
"src": "https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/1a2ef5bd-81d4-4bc2-99bb-55c250b221d1/G6+Pet+Friendly+Lifestyle.jpg?content-type=image%2Fjpeg",
"alt": "SR22T Exterior"
},
{
"src": "https://images.squarespace-cdn.com/content/66bd0ff9d4abd8302892103f/6b935415-45d7-4bb6-83ef-9296ed429310/SR+Series+NY+2024.jpeg?content-type=image%2Fjpeg",
"alt": "SR22T Lifestyle"
}
],
"availableShares": 15,
"_dataVerified": "All figures confirmed by Raymond 2026-09-12. Program cost and annual fee corrected this session (per share; stored at full precision, engine rounds for display). totalShares 16 / availableShares 15 confirmed. FUEL AND USAGE FIGURES ARE CORRECT AND CONSERVATIVE BY DESIGN \u2014 18 gph, $2.00/hr oil, $110/hr fallback, 96-hour annual cap per share. Do NOT \"correct\" them upward to match book performance figures; the conservatism is intentional.",
"addendumHtml": "<p>The initial Program aircraft may be a Cirrus SR22T GTS G6 or G7, depending on availability. The spec'd aircraft will be acquired when a\n 2026 (or newer) aircraft becomes available from Cirrus Aircraft.</p> \n <p>*{{totalShares}} total shared ownership positions, {{availableShares}} available for purchase{{remainingClause}}. Each share permits up to {{maxHours}} flying hours per year. Applicable taxes related to the aircraft purchase are not included.</p>\n <p>**The Annual Program Fee includes the bop Aero management fee, estimated aircraft maintenance, and fixed costs including insurance and storage.</p> \n <p id=\"fuel-note\">***{{fuelLabel}} national avg \u00d7 {{gph}} gph + oil. TKS Anti-ice fluid additional when used.</p>\n <p>****Estimated cost per hour does not include operational costs such as fuel or a pilot (if applicable). Year 1 includes the Program Cost and Annual Program Fee; additional years include the Annual Program Fee only. Estimated cost per hour does not factor the eventual sale of the ownership share, which may reduce the owner\u2019s realized net cost.</p>",
"map": {
"cities": [
{
"name": "Dallas",
"lat": 32.7767,
"lng": -96.797
},
{
"name": "Cincinnati",
"lat": 39.1031,
"lng": -84.512
},
{
"name": "Charlotte",
"lat": 35.2271,
"lng": -80.8431
},
{
"name": "Orlando",
"lat": 28.5383,
"lng": -81.3792
},
{
"name": "Van Nuys",
"lat": 34.1897,
"lng": -118.4493
},
{
"name": "Denver",
"lat": 39.7392,
"lng": -104.9903
},
{
"name": "Houston",
"lat": 29.7604,
"lng": -95.3698
},
{
"name": "Blue Ridge",
"lat": 34.864,
"lng": -84.3241
}
],
"serviceArea": {
"model": "radius",
"miles": 207,
"legendTitle": "Program Area",
"legendText": "Within 1 hour flight time (207 miles) of the selected city",
"dotLabel": "Owner / interest location"
}
},
"sharesRemaining": 14,
"hoursPlaceholder": "e.g., 90 hrs."
}
};

  function mount(el) {
    if (el.getAttribute('data-bopaero-mounted')) return;
    var id = el.getAttribute('data-aircraft');
    var spec = DATA[id];
    if (!spec) {
      console.error('[bopaero:' + NAME + '] unknown data-aircraft=' + JSON.stringify(id) +
                    '. Available: ' + Object.keys(DATA).join(', '));
      return;
    }
    el.setAttribute('data-bopaero-mounted', '1');
    if (CSS && !document.getElementById(STYLE_ID)) {
      var st = document.createElement('style');
      st.id = STYLE_ID; st.textContent = CSS;
      document.head.appendChild(st);
    }
    el.innerHTML = HTML;
    var root = el.querySelector('[data-calc-root]') || el;
    try { initCalculator(root, spec); }
    catch (e) { console.error('[bopaero:' + NAME + '] ' + id + ' failed', e); }
  }

  var DEPS = [];

  function loadDeps(done) {
    /* Squarespace blocks used to carry their own <script> tags for Leaflet etc.
       The stub cannot, so the loader pulls them itself — once per page, even if
       two components need the same library. */
    var pending = DEPS.length;
    if (!pending) return done();
    DEPS.forEach(function (d) {
      var sel = d.type === 'css' ? 'link[href="' + d.url + '"]' : 'script[src="' + d.url + '"]';
      var existing = document.querySelector(sel);
      if (existing) {
        if (d.type === 'css' || existing.getAttribute('data-loaded')) { if (!--pending) done(); return; }
        existing.addEventListener('load', function () { if (!--pending) done(); });
        return;
      }
      var el;
      if (d.type === 'css') {
        el = document.createElement('link'); el.rel = 'stylesheet'; el.href = d.url;
        document.head.appendChild(el); if (!--pending) done(); return;
      }
      el = document.createElement('script'); el.src = d.url; el.async = false;
      el.addEventListener('load', function () { el.setAttribute('data-loaded', '1'); if (!--pending) done(); });
      el.addEventListener('error', function () {
        console.error('[bopaero:' + NAME + '] dependency failed to load: ' + d.url);
        if (!--pending) done();
      });
      document.head.appendChild(el);
    });
  }

  function boot() {
    var els = document.querySelectorAll('[data-embed="' + NAME + '"][data-aircraft]');
    if (!els.length) {
      console.warn('[bopaero:' + NAME + '] no [data-embed="' + NAME + '"][data-aircraft] mount found');
      return;
    }
    loadDeps(function () { Array.prototype.forEach.call(els, mount); });
  }

  /* ── engine (verbatim from src/component.js) ───────────────────────── */
  /* Shared cost-calculator engine.
   *
   * One engine, one template, one stylesheet — every aircraft program is a JSON
   * file in data/. A UX or logic change here updates every calculator on the site.
   * The ONLY per-aircraft variation lives in data/<id>.json.
   *
   * Adding an aircraft: drop in data/<id>.json, rebuild, and embed a stub with
   * data-aircraft="<id>". No code changes.
   */
  function initCalculator(root, spec) {
    if (!root || !spec) return;

    var HR = ' /hr';
    var FUEL_PRICES_URL =
      'https://raw.githubusercontent.com/bopaero/bopaeroFlightCalc/main/data/fuel_prices.json';

    var money0 = function (n) {
      return n.toLocaleString('en-US', { minimumFractionDigits: 0, maximumFractionDigits: 0 });
    };
    var q = function (sel) { return root.querySelector(sel); };

    function fuelCostPerHour(pricePerGal) {
      return Math.round(pricePerGal * spec.fuel.gph + spec.fuel.oilPerHour);
    }

    /* ── static content from the data file ─────────────────────────────── */
    var titleEl = q('.summary-title'); if (titleEl) titleEl.innerText = spec.summaryTitle;
    var headEl  = q('h2');             if (headEl)  headEl.innerText  = spec.heading;

    (spec.images || []).forEach(function (img, i) {
      root.querySelectorAll('[data-img="' + i + '"]').forEach(function (el) {
        el.src = img.src; el.alt = img.alt || '';
      });
    });

    /* Footnotes are per-aircraft: they state share counts, usage terms and fuel
       specifics. They MUST come from the data file — the shared template was built
       from the SF50 component, so a hard-coded block would put SF50 footnotes
       ("one of eight positions", "unlimited usage") on every other aircraft.
       Injected before #fuel-note is queried, because it lives inside this block. */
    /* Footnote prose repeats numbers that also live in structured fields, so it
       drifts: the SR22T footnote still claimed "16 available" after the program
       moved to 15 of 16. Tokens keep one source of truth — edit the data, the
       prose follows. Unknown tokens are left visible rather than blanked, so a
       typo shows up instead of silently deleting a number. */
    /* Three distinct counts: totalShares (fleet positions), availableShares
       (offered for sale) and sharesRemaining (still unsold). Older data files
       predate the third, so it falls back to "none sold yet". */
    function remaining() {
      return typeof spec.sharesRemaining === 'number'
        ? spec.sharesRemaining : spec.availableShares;
    }

    function fill(text) {
      if (!text) return text;
      var map = {
        totalShares: spec.totalShares,
        availableShares: spec.availableShares,
        sharesRemaining: remaining(),
        /* Reads redundant while nothing has sold ("8 available for purchase, 8
           currently remaining"), so the clause disappears until it carries news.
           Computed, never prose, so it cannot drift from the numbers above. */
        remainingClause: (remaining() === spec.availableShares
          ? '' : ', ' + remaining() + ' currently remaining'),
        maxHours: spec.usage && spec.usage.maxHours,
        schedulingHours: spec.usage && spec.usage.schedulingHours,
        schedulingDays: spec.usage && spec.usage.schedulingDays,
        fuelLabel: spec.fuel && spec.fuel.label,
        gph: spec.fuel && spec.fuel.gph
      };
      return text.replace(/\{\{(\w+)\}\}/g, function (whole, key) {
        var v = map[key];
        if (v === undefined || v === null) {
          console.warn('[bopaero:aircraft-calc] ' + spec.id + ' unknown token ' + whole);
          return whole;
        }
        return v;
      });
    }

    /* Hours placeholder comes from the data file. It used to be hard-coded at
       "e.g., 175 hrs." on BOTH aircraft - on the SR22T that suggested a figure
       nearly double the 96-hour per-share limit, reading as an invitation to
       exceed the entitlement the calculator then warns about. */
    var hoursInput = q('#hours-per-year');
    if (hoursInput && spec.hoursPlaceholder) hoursInput.placeholder = spec.hoursPlaceholder;

    /* Share selector. Capped at what is actually for sale - offering 16 when 14
       remain would quote a position that cannot be bought. */
    var sharesEl = q('#share-count');
    var maxSelectable = (typeof spec.sharesRemaining === 'number' ? spec.sharesRemaining
                        : (typeof spec.availableShares === 'number' ? spec.availableShares : 1));
    if (sharesEl) {
      for (var si = 1; si <= maxSelectable; si++) {
        var op = document.createElement('option');
        op.value = si;
        op.textContent = si === 1 ? '1 share' : si + ' shares';
        sharesEl.appendChild(op);
      }
      sharesEl.value = '1';
    }
    function shareCount() {
      var n = sharesEl ? parseInt(sharesEl.value, 10) : 1;
      return (!n || n < 1) ? 1 : n;
    }

    var noteEl = q('#input-note');
    if (noteEl && spec.inputNote) { noteEl.innerText = fill(spec.inputNote); noteEl.hidden = false; }

    var addendumEl = root.querySelector('.addendum');
    if (addendumEl && spec.addendumHtml) addendumEl.innerHTML = fill(spec.addendumHtml);

    /* Every share-dependent figure is rendered here, from one place. Leaving the
       static rows at one share while the per-hour figure reflected several would
       put two different quantities of aircraft on screen at once - the same class
       of inconsistency as the capped per-hour cost. */
    function renderShareFigures() {
      var n = shareCount();
      q('#share-position').innerText =
        n === 1 ? spec.sharePositionLabel
                : spec.sharePositionLabel.replace(/\(per share\)/i, '(' + n + ' shares)');
      q('#program-cost').innerText       = '$' + money0(spec.programCost * n);
      q('#annual-program-fee').innerText = '$' + money0(spec.annualProgramFee * n);
      var hoursRow = q('#share-hours');
      if (hoursRow && spec.usage && spec.usage.model === 'capped') {
        hoursRow.innerText = 'Up to ' + (spec.usage.maxHours * n) + ' hours';
      }
    }
    renderShareFigures();

    var fuelCostEl = q('#fuel-cost');
    var fuelNoteEl = q('#fuel-note');
    fuelCostEl.innerText = '$' + money0(spec.fuel.fallbackCostPerHour) + HR;

    /* ── share availability (only if this program publishes it) ────────── */
    var sharesRow = q('[data-shares]');
    if (sharesRow && typeof spec.availableShares === 'number' && typeof spec.totalShares === 'number') {
      q('#shares-available').innerText = remaining() + ' of ' + spec.totalShares;
      sharesRow.hidden = false;
    }

    /* ── usage model: show only the rows this program uses ─────────────── */
    var model = (spec.usage && spec.usage.model) || 'unlimited';
    root.querySelectorAll('[data-usage]').forEach(function (el) {
      el.hidden = el.getAttribute('data-usage') !== model;
    });
    if (model === 'unlimited') {
      q('#share-usage').innerText = 'Unlimited';
      q('#scheduling-limit').innerText =
        spec.usage.schedulingHours + ' flight hours or ' +
        spec.usage.schedulingDays + ' days at one time';
    } else if (model === 'capped') {
      renderShareFigures();   /* scales the entitlement row with the selection */
    } else {
      console.warn('[bopaero:aircraft-calc] unknown usage model:', model);
    }

    /* ── live fuel price ───────────────────────────────────────────────── */
    function applyFuelPrice(pricePerGal, updated) {
      fuelCostEl.innerText = '$' + money0(fuelCostPerHour(pricePerGal)) + HR;
      if (fuelNoteEl) {
        fuelNoteEl.innerText =
          '***' + spec.fuel.label + ' $' + pricePerGal.toFixed(2) + '/gal (national avg, AirNav.com' +
          (updated ? ', updated ' + updated : '') + ') × ' + spec.fuel.gph + ' gph + oil.';
      }
    }
    fetch(FUEL_PRICES_URL + '?v=' + new Date().toISOString().slice(0, 10))
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (d) {
        var p = d && d[spec.fuel.priceKey];
        if (typeof p === 'number') applyFuelPrice(p, d.updated);
      })
      .catch(function (e) {
        console.warn('[bopaero:aircraft-calc] ' + spec.id + ' fuel price fetch failed:', e.message);
      });

    /* ── interaction ───────────────────────────────────────────────────── */
    var yearsEl = q('#ownership-years');
    var hoursEl = q('#hours-per-year');
    var blendedEl = q('#blended-cost-per-hour');
    var blendedRow = q('#blended-row');
    var warnEl = q('#usage-warning');

    /* A capped program cannot deliver more than maxHours per share per year.
       Without this, entering 175 hrs/yr against a 96-hour SR22T share returns a
       confident cost per hour for a usage level the program does not permit —
       and because the figure falls as hours rise, the error flatters the wrong
       way. Warn rather than block: needing a second share is a conversation,
       not an input error. */
    /* Entitlement scales with shares: N shares carry N x maxHours per year. This is
       the same arithmetic the warning already asserted ("2 shares would be required
       at this usage level"), now that the buyer can actually select those shares. */
    function entitlement() {
      var cap = spec.usage && spec.usage.model === 'capped' ? spec.usage.maxHours : null;
      return cap ? cap * shareCount() : null;
    }
    function overCap(hoursPerYear) {
      var allowed = entitlement();
      return allowed && hoursPerYear > allowed ? allowed : null;
    }

    function checkUsageCap(hoursPerYear) {
      if (!warnEl) return;
      var cap = spec.usage && spec.usage.model === 'capped' ? spec.usage.maxHours : null;
      var allowed = entitlement();
      if (allowed && hoursPerYear > allowed) {
        var have = shareCount();
        var need = Math.ceil(hoursPerYear / cap);
        warnEl.innerText =
          hoursPerYear + ' hours per year exceeds the ' + allowed + '-hour annual limit for ' +
          (have === 1 ? 'a single share' : have + ' shares') + '. ' + need +
          (need === 1 ? ' share' : ' shares') + ' would be required at this usage level — ' +
          'contact us to discuss.';
        warnEl.hidden = false;
      } else {
        warnEl.hidden = true;
      }
    }

    /* The photos are loading="lazy" with height:auto and no intrinsic size, so while
       they have zero area the browser may never schedule the load - and opening the
       accordion does not by itself break that deadlock. Switching to eager and
       re-triggering guarantees the fetch.
       MUST run at init as well as on toggle: the accordion now ships open, so the
       toggle event never fires on load and the images would never wake. */
    function wakeImages(scope) {
      scope.querySelectorAll('img[loading="lazy"]').forEach(function (img) {
        img.loading = 'eager';
        if (!img.complete || !img.naturalWidth) { var u = img.src; img.src = ''; img.src = u; }
      });
    }
    if (root.open) wakeImages(root);

    root.addEventListener('toggle', function () {
      if (!this.open) return;
      wakeImages(this);
      /* Only pull the page to the calculator when the USER opened it. Doing this on
         an accordion that ships open would yank the page on load. */
      if (this.dataset.userToggled) this.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });

    function calculate() {
      var years = Number(yearsEl.value);
      var hoursPerYear = Number(hoursEl.value);
      if (!Number.isInteger(years) || years < 1 || years > 20) {
        alert('Please enter Years of Ownership as a whole number between 1 and 20.'); return;
      }
      if (!Number.isFinite(hoursPerYear) || hoursPerYear < 1) {
        alert('Please enter estimated annual flying hours greater than zero.'); return;
      }
      /* Above the cap the per-hour figure is not just unhelpful, it is wrong in the
         flattering direction: it divides ONE share's cost by hours that one share is
         not entitled to fly, so the rate falls as the overage grows. Raymond's call
         (2026-09-14): show the warning alone rather than a number a prospect could
         quote back. The static per-share rows stay - they remain true. */
      var cap = overCap(hoursPerYear);
      if (cap) {
        blendedEl.innerText = '-';
        if (blendedRow) blendedRow.hidden = true;
      } else {
        /* ASSUMPTION, flagged to Raymond 2026-09-15: buying N shares costs N x the
           program cost and N x the annual fee. Linear, and consistent with the cap
           warning that already told prospects they would need a second share. If the
           real pricing is not linear this is the one place to change it. */
        var total = (spec.programCost + spec.annualProgramFee * years) * shareCount();
        blendedEl.innerText = '$' + money0(total / (years * hoursPerYear)) + HR;
        if (blendedRow) blendedRow.hidden = false;
      }
      checkUsageCap(hoursPerYear);
    }

    function clearData() {
      yearsEl.value = ''; hoursEl.value = '';
      blendedEl.innerText = '-';
      if (blendedRow) blendedRow.hidden = false;
      if (warnEl) warnEl.hidden = true;
      yearsEl.focus();
    }

    var summaryEl = root.querySelector('summary');
    if (summaryEl) summaryEl.addEventListener('click', function () { root.dataset.userToggled = '1'; });

    if (sharesEl) sharesEl.addEventListener('change', function () {
      renderShareFigures();
      /* Only recompute if the visitor has already produced a result; otherwise a
         stale per-hour figure would appear from an empty form. */
      if (yearsEl.value && hoursEl.value) calculate();
    });

    q('#calculate-cost').addEventListener('click', calculate);
    q('#clear-calculator').addEventListener('click', clearData);

    [yearsEl, hoursEl].forEach(function (el) {
      el.addEventListener('keydown', function (e) {
        if (e.key === 'Enter') { e.preventDefault(); calculate(); el.blur(); }
      });
      el.addEventListener('blur', function () {
        if (yearsEl.value && hoursEl.value) calculate();
      });
    });
  }

  if (document.readyState === 'loading')
    document.addEventListener('DOMContentLoaded', boot);
  else boot();
})();
